// console.log(truePositions);
const validation = () => {
  truePositions.forEach((word) => {
    console.log(word);
  });

  if (toValidate.includes("L0_C0")) {
    // console.log("coisa nenhuma");
  }
};
